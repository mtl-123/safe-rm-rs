# 忽略所有文件（默认规则）
*

# 反向保留核心文件（仅这三个）
!main.rs
!Cargo.toml
!Cargo.lock

# 保留目录结构（确保核心文件所在目录可被Git识别，避免空目录忽略问题）
!./

# ===================== 可选：忽略通用无关文件（按需保留）=====================
# 编辑器/IDE临时文件（如需保留可删除以下行）
*.swp
*.swo
*.tmp
.idea/
.vscode/
*.iml

# 系统临时文件（如需保留可删除以下行）
.DS_Store