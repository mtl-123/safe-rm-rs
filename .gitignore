# ===================== Rust 编译产物（核心忽略）=====================
# 忽略 Rust 编译后的所有产物（包括 debug/release 目录）
target/

# 忽略 Cargo 本地缓存/锁文件（可选，按需保留）
# Cargo.lock 建议保留（确保依赖版本一致），如需忽略可取消注释
# Cargo.lock
.cargo/
vendor/
registry/

# ===================== 临时/无关文件（可选忽略）=====================
# 编辑器临时文件
*.swp
*.swo
*.tmp
.DS_Store
.idea/
.vscode/
*.log

# 本地打包临时目录（仅 Actions 构建用，不上传）
release/
dist/

# ===================== 关键：不忽略 .github/ 目录 =====================
# 无需添加任何 .github/ 相关规则 → Git 会自动跟踪该目录
